const numero1 = 20;
const numero2 = "20";


console.log( parseInt(numero2)); // es una funcion va primero que la variable y no tiene puento

console.log(numero1.toString());// es un metodo va primero la variable y contiene punto
